어레이
주소값 참조를 못함
임의의 주소를 지정했다고 가정


순차적으로 저장되어있음. 딱 붙어서.
조회시 한번의 연산만 있으면된다 조회시O(1)
중간에 밀어내고 삭제하고 하는게 아니라면

!@#@!##########중간이 아닌 추가 삭제 업뎃시...0(1), 중간이면 O(N)

캐시히트율높음
성능이 제일 좋다. 배열 범위넘어가면 예외뜸



리스트
타입추론이 가능해서 변수쪽에만 쓰면됨.
공간은 최소값 40바이트를 갖고있음
10개넘어가면 (자체적으로 리사이즈때림)
그래서 정적인 배열을 마치 동적인것 처럼 보이는 것.

!@#!###################만약 중간에 값을 넣거나 삭제한다면 배열의 값을 뒤로 밀어야함 이럴떄는 O(n)

수정은 중간거 찾아서 값을 바꾸면 되니 O(1)

링크드리스트
노드가 끊어져있다 (주소값이 퍼뜨려져있다.)
노드에 값이랑 다음번노드의 값이 저장되어있음
조회하려면 앞에서부터 계쏙 찾아야됨 중간게 어딨는지 모르니까 O(N)

삽입 삭제노드찾아가는과정에서 O(n)연산을 해야함

그래서 다 불리함 근데 왜씀? 대기나 큐로 만듬 원형링크드리스트로 앞에꺼 넣을때 O(1)이라 쓰는거임 ㅇㅇ